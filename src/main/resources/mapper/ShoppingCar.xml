<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dao.ShoppingCarDAO">

    <resultMap  type="com.example.domain.ShoppingCar" id="carInfo">
        <id column="id"  property="id" />
        <id column="user_id"  property="userId" />
        <result column="product_id"  property="productId" />
        <result column="product_price"  property="productPrice" />
        <result column="counts"  property="counts" />
        <result column="product_name"  property="productName" />
    </resultMap>
    <select id="querryAll" resultMap="carInfo">
        select * from shopping_car where user_id=#{id};
    </select>

    <insert id="save" parameterType="com.example.domain.UserMain" useGeneratedKeys="true" keyProperty="id">
        insert into shopping_car
        (
        `user_id`,
        `product_id`,
        `product_price`,
        `counts`,
        `create_time`,
        `product_name`
        )
        values
        (
        #{userId},
        #{productId},
        #{productPrice},
        #{counts},
        #{createTime},
        #{productName}
        )
    </insert>

    <delete id="delete">
        delete from shopping_car where id = #{id}
    </delete>

    <select id="queryTotal" resultType="int">
        select count(*) from shopping_car where user_id = #{id}
    </select>
</mapper>

